<!DOCTYPE html>
<html>
  <head>
    <title style="font-family: cursive;" >MusicApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <%= yield %>
    <br>
    <% if logged_in? %>
      <h2> <%= current_user.username %> </h2>
      <form action="<% session_url %>" method="POST"></form>
        <input type="hidden" name="_method" value="delete">
        <button>Logout</button>
      </form>
      <% else %>
      <form action="<%= new_session_url %>" method="GET">
        <button style="border: solid grey;border-radius: 50px 50px 50px 50px; color: aqua; background-color: black; display: flex; justify-content: flex-end; align-items: flex-end;" >Login</button> <!--- extra log in? not working. -->
      </form>
    <% end %>
  </body>
</html>
